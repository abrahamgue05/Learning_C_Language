#include <stdio.h>

int main() {
    //Coment:Block of code that gets ignored when we run a program
    /*
    Multiline comments, can storage more than one 
    line of comments. Open /* Close */
    //Single line comment, ca storage one line of comments
    printf("Comments are fun");
    return 0;
}