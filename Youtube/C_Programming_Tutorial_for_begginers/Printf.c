#include <stdio.h>

int main() {
    
    double favNum = 90.1;
    char myChar = 'i';

    printf("My favorite %c is %f", myChar, favNum); //Print what is inside the ()
    //order it will appear on screen
    //%d integers
    //%s Text
    //%f decimals/double
    //%c characters 
    return 0;
}