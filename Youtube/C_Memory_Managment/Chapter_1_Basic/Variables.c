#include <stdio.h>

int main(){

    int x = 5;

    printf("That's declaring a variable %d\n", x);

    x = 10;

    printf("This changes its value but its still posible %d\n", x);

    //float x = 12;
    //printf("Now this is not posible %f\n", x);

    return 0;
}